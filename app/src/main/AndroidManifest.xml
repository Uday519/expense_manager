<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expenses">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_custom_colored"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_custom_colored_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity" android:theme="@style/AppFullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" >

        </activity>
        <activity android:name=".NewExpense" android:theme="@style/AppTheme"
            android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".Statistics" android:theme="@style/AppTheme"
            android:parentActivityName=".MainActivity">

        </activity>
    </application>

</manifest>